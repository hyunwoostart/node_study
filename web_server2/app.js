const express = require('express'); // npm install express --save
const app = express();

app.listen(3000, () => {
    console.log('3000번에 귀를 대고 듣기 시작했음');
});

// 처리해주는 루틴들을 추가...
app.get()

app.get()

app.post()

app.post();
app.post();
app.post();
app.post();